# Uncomment this line to define a global platform for your project
platform :ios, '13.0'

# A Podfile template that automatically includes all Flutter plugins

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Pods for plugins
  flutter_install_all_ios_pods
end
